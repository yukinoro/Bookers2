<html>
<body>
 <div class='body'>
  <div class='container'>
  <div class="notice">
  <% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
  <% end %>
   </div>
    <div class='row'>
      <div class='col-xs-3'>
                 <h2>User info</h2>
                <table class='table'>
                   <tr>
                     <th><%= attachment_image_tag @book.user, :profile_image , :fill, 60, 60, fallback: "no_image.jpg", class: "img-circle pull-left profile-thumb" %></th>
                   </tr>
                  <tr>
                    <th><%= "name" %></th>
                    <th><%= @book.user.name %></td>
                  </tr>
                  <tr>
                    <th><%= "introduction" %></th>
                    <th><%= @book.user.introduction %></th>
                  </tr>
            </table>
             <% if @book.user.id == current_user.id %>
                   <div class='row'>
                    <%= link_to "", edit_user_path(@book.user), class: "btn btn-default glyphicon glyphicon-wrench col-xs-12" %>
                   </div>            
                <% end %>

                <h1>New Book</h1>
                <%= form_for(@book_new) do |f| %>
                    <h4>title</h4>
                    <%= f.text_field :title %>
                    <h4>opinion</h4>
                    <%= f.text_area :body %>
                    <%= f.submit 'Create book' %>
                <% end %>
    　</div>


   　 <div class='col-xs-9'>
                <h2>Book detail</h2>
              <table class='table table-hover table-inverse'>
              <thead>
                <tr> 
                  <th></th>
                  <th></th>
                  <th></th>
                  <th colspan="3"></th>
                </tr>
              </thead>
              <tbody>
                    <tr>
                      <td>
                        <%= attachment_image_tag @book.user, :profile_image , :fill, 30, 30, fallback: "no_image.jpg", size:'30x30' , class: "img-circle profile-thumb" %>
                        <br>
                        <%= link_to user_path(@book.user) do %>
                          <%= @book.user.name %>
                        <% end %>
                      </td>
                      <td><%= @book.title %></td>
                      <td><%= @book.body %></td>
                    <% if @book.user_id == current_user.id %>
                      <td>
                        <%= link_to "Edit", edit_book_path(@book),class: "edit_#{@book.id}" %>
                      </td>
                      <td>
                        <%= link_to "Destroy", book_path(@book.id), method: :delete,class: "destroy_#{@book.id}" %>
                      </td>
                      <% end %>
                    </tr>
              </tbody>
              </table>
      </div>
    </div>
  </div>
</div>

    <footer>
  <div class='container'>
    <div class='row'>
      <div class='col-xs-3 col-xs-offset-9'>
        <p>CopyRight Infratop.inc</p>
      </div>
    </div>
  </div>
</footer>


  </body>
</html>
