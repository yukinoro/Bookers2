<html>
<div class='body'>
  <div class='container'>
    <div class="notice">
  <% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
  <% end %>
   </div>
    <div class='row'>
      <div class='col-xs-3'>
                 <h2>User info</h2>
                <table class='table'>
                   <tr>
                     <th><%= attachment_image_tag @user, :profile_image , :fill, 60, 60, fallback: "no_image.jpg", size:'60x60' , class: "img-circle pull-left profile-thumb" %></th>
                   </tr>
                  <tr>
                    <th><%= "name" %></th>
                    <th><%= @user.name %></th>
                  </tr>
                  <tr>
                    <th><%= "introduction" %></th>
                    <th><%= @user.introduction %></th>
                  </tr>
            </table>
             <% if @user.id == current_user.id %>
             <div class='row'>
                    <%= link_to "", edit_user_path(@user), class: "btn btn-default glyphicon glyphicon-wrench col-xs-12" %>
                  </div>
            <% end %>

           <h1>New Book</h1>
            <%= form_for(@book) do |f| %>
                <h4>title</h4>
                <%= f.text_field :title %>
                <h4>opinion</h4>
                <%= f.text_area :body %>
                <%= f.submit 'Create book' %>
            <% end %>
    　</div>

      <div class='col-xs-9'>
              <h2>Books</h2>
              <table class='table table-hover table-inverse'>
              <thead>
                <tr> 
                  <th></th>
                  <th>Title</th>
                  <th>Opinion</th>
                  <th colspan="3"></th>
                </tr>
              </thead>
              <tbody>
                    <% @books.each do |book| %>
                    <tr>
                      <td></td>

                      <td><%= link_to "#{book.title}",book_path(book)%></td>
                      <td><%= book.body %></td>
                    </tr>
                    <% end %>
              </tbody>
              </table>
      </div>
  </div>
 </div>
</div>

<footer>
  <div class='container'>
    <div class='row'>
      <div class='col-xs-3 col-xs-offset-9'>
        <p>CopyRight Infratop.inc</p>
      </div>
    </div>
  </div>
</footer>




  </body>
</html>
